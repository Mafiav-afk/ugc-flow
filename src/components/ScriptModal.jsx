import { Save, X } from 'lucide-react'
import { useState } from 'react'
export function ScriptModal({ initialShots, onClose, onSave }) { const [shots, setShots] = useState(initialShots); return <div className="modal-backdrop"><div className="modal script-modal" role="dialog" aria-modal="true" aria-label="编辑 8 镜头脚本"><div className="modal-head"><div><strong>编辑 8 镜头脚本</strong><small>保持每个镜头约 2 秒，第 8 镜头为 1 秒 CTA</small></div><button aria-label="关闭脚本编辑" onClick={onClose}><X/></button></div><div className="script-editor">{shots.map((shot, index) => <label key={index}><span>{index + 1}<time>{String(index * 2).padStart(2, '0')}:00–{String(Math.min(15, index * 2 + 2)).padStart(2, '0')}:00</time></span><textarea value={shot} onChange={(event) => setShots((current) => current.map((item, itemIndex) => itemIndex === index ? event.target.value : item))}/></label>)}</div><div className="modal-actions"><button className="primary" onClick={() => onSave(shots)}><Save size={15}/>保存脚本</button></div></div></div> }
